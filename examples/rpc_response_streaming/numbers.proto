syntax = "proto3";

package numbers;

service NumberGenerator {
  // Client sends one request with max number, server streams back numbers
  rpc GenerateNumbers(NumberRequest) returns (stream NumberResponse) {}
}

message NumberRequest {
  int32 max = 1; // client wants numbers up to this
}

message NumberResponse {
  int32 number = 1; // each response contains one number
}
